#define MAXLINE 1000
#include <stdio.h>
#include <string.h> 
#include <ctype.h>


#define SIZE 100
main()
{
    char s[SIZE];
    int i, c;

    for (i = 0; s[++i] = c = getchar(); i++) 
        printf("i: %i, value: %d \n", i, s[i]);
}